version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd calendar_frontend
        - npm ci
    build:
      commands:
        - npm run build --prod
  artifacts:
    baseDirectory: calendar_frontend/dist/calendar_frontend
    files:
      - '**/*'
  cache:
    paths:
      - calendar_frontend/node_modules/**/*